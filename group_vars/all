---
# Keep this file intentionally small. Only cross-host defaults that are still
# used by the active code path belong here.

# The common operator account used by the active base/server roles.
desktop_user: lion

# Most hosts can use the normal system Python. Hosts that need something else
# should override this in their own host_vars.
ansible_python_interpreter: /usr/bin/python3
